<%= @post.title%>
<%= @post.content %>
<% @post.comments.each do |comment| %>
    <%= comment.content%>
<% end %>
<% @post.comments.each do |comment| %>
    <%= link_to comment.user.username, user_path(comment.user) %>
<% end %>